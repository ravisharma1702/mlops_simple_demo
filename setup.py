from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    description = "Case study",
    author = "rsharma",
    packages = find_packages(),
    licnese = "MIT"

)